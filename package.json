{
  "name": "@moritzbrantner/philosophy-lab",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun web/index.html --console",
    "build": "rm -rf dist && bun build web/index.html --outdir=dist --target=browser",
    "format": "bunx oxfmt@0.66.0 web/src tests package.json tsconfig.json .coding-tooling.json .coding-tooling.expectations.json renovate.json .devcontainer/devcontainer.json",
    "format:check": "bunx oxfmt@0.66.0 --check web/src tests package.json tsconfig.json .coding-tooling.json .coding-tooling.expectations.json renovate.json .devcontainer/devcontainer.json",
    "lint": "bunx oxlint@1.81.0 web/src tests",
    "typecheck": "bunx -p typescript@7.0.2 tsc -p tsconfig.json --noEmit",
    "test": "bun test tests/unit",
    "test:unit": "bun test tests/unit",
    "test:integration": "bun test tests/integration",
    "verify": "bun run format:check && bun run lint && bun run typecheck && bun run test:unit && bun run test:integration && bun run build"
  },
  "packageManager": "bun@1.4.0"
}
